{
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "build": "docker-compose build && docker-compose up"
  },
  "dependencies": {
    "express": "^4.16.3",
    "mongoose": "^6.8.0"
  }
}
