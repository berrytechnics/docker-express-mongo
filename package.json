{
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "build": "npm install && docker-compose build"
  },
  "dependencies": {
    "express": "^4.16.3",
    "mongoose": "^6.8.0"
  }
}
